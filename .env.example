# 本地开发默认配置（可选，Token 仍建议在页面中配置）
# VITE_DEFAULT_OWNER=your-github-username
# VITE_DEFAULT_REPO=photoBed
# VITE_DEFAULT_BRANCH=main
# VITE_IMAGES_DIR=images

# 构建 GitHub Pages 时在 CI 中设置，本地模拟 Pages 可执行：
# set VITE_BASE_PATH=/photoBed/ && npm run build
